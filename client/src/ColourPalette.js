import { createMuiTheme } from "@material-ui/core/styles";

export const palette = createMuiTheme({
  blueDark: "#102542",
  redLight: "#F87060",
  grayLight: "#cdd7d6",
  brownLight: "#B3a394",
  white: "#FFFFFF",
});
